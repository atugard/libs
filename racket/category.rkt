(define (category objects morphisms)
  (display tbd))

(define cat (category {1,-1} {(lambda (x) (expt -1 x)}))
  ;;if morphism, give new morphism satisfying particular rules.
  ;;if object, give new object satisfying particular rules.
