;;We could define trees recursively. T1 = (T2, T3), where those two are one unit shorter, and so on, until we hit leaves, and then use those leaves to make our tree, and so on. 
(define (tree r . subtrees)
  )



;;(a+b)^p. A choice tree of height p gives you all the info to compute this. 




